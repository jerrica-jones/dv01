export type LoanData = {
    year: string;
    quarter: string;
    grade: string;
    homeOwnership: string;
    term: string;
    currentBalance: string;
};

export type LoanGraphData = {
    grade: string;
    amount: number;
};